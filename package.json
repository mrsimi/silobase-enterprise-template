{
  "name": "silobase-enterprise",
  "version": "1.0.0",
  "description": "Silobase deployment on Render",
  "scripts": {
     "start": "echo $SILOBASE_LICENSE_BASE64 | base64 -d > /tmp/license.lic && silobase-enterprise start --license /tmp/license.lic"
  },
  "dependencies": {
    "silobase-enterprise": "1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
